name: setup-go-workspace
version: 1.0.0
description: |
  Adds the project to the Go workspace directory hierarchy.
  You can set the `dir` option to override the default package directory that is
  the git provider url + user + project, eq: `github.com/pjvds/go-logging`
